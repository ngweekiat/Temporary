Function ExtractFirstTextNode(ByVal html As String, ByVal cssSelector As String) As String
    Dim htmlDoc As Object
    Dim elem As Object
    
    Set htmlDoc = CreateObject("HTMLFILE")
    htmlDoc.body.innerHTML = html
    
    Set elem = htmlDoc.querySelector(cssSelector)
    If Not elem Is Nothing Then
        If elem.firstChild.nodeType = 3 Then  ' 3 = text node
            ExtractFirstTextNode = Trim(elem.firstChild.nodeValue)
        Else
            ExtractFirstTextNode = vbNullString
        End If
    Else
        ExtractFirstTextNode = vbNullString
    End If
End Function
